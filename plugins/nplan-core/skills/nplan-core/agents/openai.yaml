interface:
  display_name: "NPlanCore"
  short_description: "Safety-gated plans and governed memory"
  default_prompt: "Use $nplan-core to safety-check this request, create a bounded plan, and report independent validation status without executing it."
